import 'package:basureward/packages.dart';

class NotificationHomeView extends StatelessWidget {
  const NotificationHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
