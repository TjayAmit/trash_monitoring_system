import 'package:basureward/packages.dart';

class ReportComposeView extends StatelessWidget {
  const ReportComposeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
