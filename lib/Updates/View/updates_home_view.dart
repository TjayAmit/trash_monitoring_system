import 'package:basureward/packages.dart';

class UpdatesHomeView extends StatelessWidget {
  const UpdatesHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
