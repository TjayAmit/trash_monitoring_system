import 'package:basureward/packages.dart';

class RewardsHomeView extends StatelessWidget {
  const RewardsHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
