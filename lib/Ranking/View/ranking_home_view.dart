import 'package:basureward/packages.dart';

class RankingHomeView extends StatelessWidget {
  const RankingHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
